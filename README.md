# mtai AI Agent

## Phase 3 storage configuration

The worker supports an optional Cloudflare KV binding named `MARKET_DATA_STORE`. No namespace ID or credential is committed to this repository. Without the binding, tests and local execution use an in-memory store.
